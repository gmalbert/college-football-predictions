# FastAPI + React parity build — additional runtime and test dependencies.
#
# Install on top of the Streamlit stack:
#     python -m pip install -r requirements-web.txt
#
# The API imports `utils/` directly (including utils/config.py and
# utils/ui_components.py, which import streamlit), so the Streamlit
# requirements come first.

-r requirements.txt

# --- API server -------------------------------------------------------------
# fastapi 0.118 is the newest release that still accepts pydantic v1, which
# cfbd 5.27 pins (cfbd requires pydantic<2). It also allows the
# starlette>=0.46 that streamlit 1.63 requires.
fastapi==0.118.0
uvicorn>=0.30.0
httpx>=0.27.0

# api/main.py uses Pillow directly to downscale data_files/logo.png before
# serving it through /api/logo. It currently also arrives transitively via
# streamlit, but it is a direct import and is pinned here so that stays true.
Pillow>=10.0.0

# --- Tests ------------------------------------------------------------------
pytest>=8.0.0
playwright>=1.45.0

# After installing, fetch the browser used by the Playwright suites:
#     python -m playwright install chromium
